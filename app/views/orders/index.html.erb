
<div class="main-container">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="fw-bold text-primary">POSly: Inventory Dashboard</h1>
    <%= link_to "+ New Order", new_order_path, class: "btn btn-success btn-lg shadow-sm" %>
  </div>

  <div style="margin-bottom: 20px; display: flex; gap: 10px; align-items: center;">
  <%= form_with url: orders_path, method: :get, local: true, class: "search-form" do |f| %>
    <%= f.text_field :query,
        value: params[:query],
        placeholder: "พิมพ์เลขที่ใบเสร็จหรือชื่อลูกค้า...",
        style: "padding: 10px; width: 300px; border: 1px solid #ccc; border-radius: 4px; font-size: 16px;"
    %>
    <%= f.submit "Search",
        style: "padding: 10px 20px; background-color: #007bff; color: white; border: none; border-radius: 4px; cursor: pointer; font-size: 16px;"
    %>

    <% if params[:query].present? %>
      <%= link_to "Clear", orders_path, style: "margin-left: 10px; color: #666; text-decoration: none;" %>
    <% end %>
  <% end %>
</div>

<%# Turbo Frame ครอบตารางสินค้า %>
<%= turbo_frame_tag "products" do %>
<div class="table-responsive shadow-sm rounded">
    <!-- สร้างให้ท้ังแถวสามารถคลิกแล้วลิ้งไปที่หน้าโชว์รายละเอียด -->
    <table class="table table-hover table-bordered align-middle">
      <thead class="table-light">
        <tr class="text-center">
          <th style="width: 15%;">Order No.</th>
          <th style="width: 15%;">Date</th>
          <th style="width: 30%;">Customer Name</th>
          <th style="width: 15%;">Total Price</th>
          <th style="width: 15%;">Payment Method</th>
          <th style="width: 10%;">Status</th>
        </tr>
      </thead>
      <tbody>
        <% @orders.each do |order| %>
          <tr class="clickable-row" data-href="<%= order_path(order) %>" style="cursor: pointer;">
            <td class="text-start"><%= order.order_number %></td>
            <td class="text-start"><%= order.created_at.strftime("%d/%m/%Y %H:%M") %></td>
            <td class="text-start text-muted"><%= order.customer_name %></td>
            <td class="text-end text-muted"><%= number_to_currency(order.order_items.sum { |item| item.unit_price * item.quantity }, unit: "B", precision: 2, format: "%n %u") %></td>
            <td class="text-center fw-medium"><%= order.payment_method %></td>
            <td class="text-center text-muted"><%= order.status %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
      <% if @orders.none? %>
        <div class="text-center py-5 text-muted">
          <i class="fa-solid fa-box-open fa-3x mb-3"></i>
          <p>ไม่พบข้อมูลorderที่ค้นหา</p>
        </div>
      <% end %>
  </div>
  <% end %>
</div>


<script>
  // เปลี่ยนจาก DOMContentLoaded เป็น turbo:load
  document.addEventListener("turbo:load", () => {
    const rows = document.querySelectorAll(".clickable-row");
    rows.forEach(row => {
      row.addEventListener("click", () => {
        const url = row.dataset.href;
        if (url) {
          // ใช้ Turbo.visit แทน window.location เพื่อความเร็วและสมบูรณ์ของระบบ Turbo
          Turbo.visit(url);
        }
      });
    });
  });
</script>
<!--
<script>
  document.addEventListener("DOMContentLoaded", () => {
    const rows = document.querySelectorAll(".clickable-row");
    rows.forEach(row => {
      row.addEventListener("click", () => {
        // เมื่อคลิก จะดึงค่าจาก data-href แล้วเปลี่ยนหน้าเว็บไปที่นั่น
        window.location.href = row.dataset.href;
      });
    });
  });
</script>
-->
